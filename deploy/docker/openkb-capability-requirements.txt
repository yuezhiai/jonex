# [jonex] Jonex Atomic Capability 运行时最小依赖
# 注意：不 pin pydantic / fastapi / openai 版本，避免与 OpenKB uv.lock 冲突
fastapi
uvicorn[standard]
PyJWT>=2.8.0
python-dotenv>=1.0.0
pyyaml>=6.0.1
redis>=5.0.0
httpx>=0.27.0
python-json-logger>=2.0.7

# [jonex] jonex_core.common 急切导入链所需(database/neo4j/security);均不依赖 pydantic,
# 不与 OpenKB 的 pydantic v2 / litellm 冲突。pymilvus/qcloud_cos/boto3 在源码里 try/except 保护,无需装。
sqlalchemy>=2.0.25
greenlet>=3.0.0
asyncpg>=0.29.0
neo4j>=5.25.0,<6.0.0
bcrypt>=4.0.0
